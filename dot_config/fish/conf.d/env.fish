################## NOTICE ##################
# Since we are calling `$SHELL /usr/bin/fish` instead of setting fish as the
# user shell, these variables are now set in ~/.profile (for dash) and
# and sourced by ~/.bash_profile, then inherited by fish.
############################################
